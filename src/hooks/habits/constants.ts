
// Minimum hours between streak days for habit completion
export const MIN_HOURS_BETWEEN_STREAK_DAYS = 8;

// Toast display durations
export const TOAST_DURATIONS = {
  SHORT: 3000,
  NORMAL: 5000,
  LONG: 8000
};

// Habit operation types
export enum HabitOperation {
  ADD = 'add',
  UPDATE = 'update',
  DELETE = 'delete'
}
